import React from "react";

const NoMatch = () => {
    return (
        <div>
            Sorry...that page seems to have ran away and we can't find it =/
        </div>
    );
};

export default NoMatch;